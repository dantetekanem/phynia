<!DOCTYPE html>
<html>
  <head>
    <title>Phynia</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "battle", "data-turbo-track": "reload" if params[:controller] === "battle" && params[:action] === "index" %>
    <%= javascript_importmap_tags %>
    <%= yield(:head) %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
